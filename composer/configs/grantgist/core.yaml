defaults:
  - _self_
  - paths: default.yaml
  - hydra: default.yaml
  - protocol: default.yaml
  - ai: ollama.yaml

dryrun: false

# Used to pull the latest information at all times
today: ${now:%Y%m%d}

# Prompting for the llms
# Set to None by default
prompt: null

force: false

n_jobs: 8

hydra: 
  searchpath:
    - pkg://composer.configs

